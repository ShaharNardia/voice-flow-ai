# Optional: copy to .env in this folder.
# - node fullTestSuite.js loads it automatically (quiet).
# - bash tests/run_all_tests.sh sources it before the API step.
# Use real Firebase users only; placeholders will fail Auth and exit non-zero.

QA_EMAIL=
QA_PASSWORD=
FIREBASE_FUNCTIONS_URL=
FIREBASE_API_KEY=
CLOUD_RUN_URL=
